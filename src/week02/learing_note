*选择排序法会造成空间上的浪费
那么可否进行原地排序？
过程：arr[i...n)未排序，arr[0...i]已经排序，arr[i...n)中的最小值要放到arr[i]的位置
循环不变量为:arr[i...n)未排序，arr[0...i]已经排序

*选择排序时间复杂度：O(n方)，同时在复杂度分析中低阶项也要舍弃

*插入排序法
arr[0,i)一排好序；arr[i...n)未排序——为该排序法的循环不变量
插入排序与选择排序的区别：选择排序已经遍历的元素位置为最终的位置，插入则不同

**sort()、sort2()差别：
sort2()利用临时变量空间保存待比较元素，不需要进行swap()的三次交换，节省时间，这是个很重要的优化

*插入排序的重要特征：
对于有序数组，插入排序的复杂度是O(n),但算法复杂度考虑最坏情况，整体任然为O(n方)